Regenerate `src/assets/vaisesika-portrait.jpg` from the source `Vaiseika-dasa-4.jpg` with only ~40px of top padding (instead of 180px), keeping the same 200×200 optimized output (~4 KB) so the avatar loads fast and sits tighter to the head.

No code changes — just the image asset.